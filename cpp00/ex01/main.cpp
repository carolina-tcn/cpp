#include <iostream>
#include "PhoneBook.class.hpp"
#include "Contact.class.hpp"

int main()
{
	PhoneBook newPhoneBook;
	string std::command;

	//print commands

	while (true)
	{
		if //command EXIT
		else if //command ADD
		else if //command SEARCH
	}
	return (0);
}
